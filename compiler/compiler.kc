include "compiler/tokenizer.kc"
include "lib/map.kc"

struct {COMPILER} = TOKENIZER{CTOK}, [CBIN];

function compiler_init(c)
{
    return TRUE;
}

function compile(c, src)
{
    format(print, "source: %s\r\n", src);
    return NULL;
}