ORG 0

MOV AX, msg
PUSH AX
MOV AX, 15
PUSH AX
CALL print
#print(15, "Hello, World!\r\n");
CLI
HLT
JMP $
msg: DB 'Hello, World!', 0xD, 0xA

function print(len, s)
{
    MOV SI, s
    while (len--)
    {
        LODSB
        MOV AH, 0x0E
        INT 0x10
    }
}
