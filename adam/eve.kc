function eve(compiler)
#(vec{VEC}, x)
{
    print("Compiled Eve\n");
    format(print, "first_free: &x: %x: %x\n", &first_free, first_free);
#    vec_init(vec, 64, 1);
#    while (TRUE)
#    {
#        print("> ");
#        input(vec);
#        x = compile(compiler, "stdin", vec);
#        if (x) x();
#    }
}

include "lib/def.kc"
include "lib/util.kc"
include "lib/fs.kc"
include "lib/malloc.kc"
include "lib/map.kc"
include "lib/vec.kc"
include "compiler/compiler.kc"